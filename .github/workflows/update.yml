name: update

on:
  schedule:
    - cron: "*/60 * * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: update
        run: |
          echo hello
          echo world
